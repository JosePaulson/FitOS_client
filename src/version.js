// Bump this with every shipped change (bug fix, feature, tweak) so the
// version shown in the sidebar always identifies exactly which build a
// staff member is running — useful for confirming someone has picked up a
// fix. Convention: semantic-ish MAJOR.MINOR.PATCH — PATCH for fixes/small
// tweaks, MINOR for new features, MAJOR for breaking/large overhauls.
export const APP_VERSION = '1.2.1'
